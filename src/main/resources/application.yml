spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/finance_db1
    username: pgdev
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    secret: blablabablablablablabababalbalbabalbabal
    expiration-ms: 3600000
